#include<stdio.h>
#include<math.h>
int main()
{
    int n=1, b, i=0, a=1;

	printf("Input the number of terms:");
	scanf("%d",&b);
		n=pow(a,3);
	while(i<("%d",b))	
	{
			n=pow(a,3);
		printf("Number is: %d and cube of the %d is:%d\n",i+1,i+1,n);
	/*	scanf("%d",&b);*/
		a++;
			i++;
	}
	return 0;
}
